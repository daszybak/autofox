import { createStyles } from '@mantine/core';
export var useStyles = createStyles(function (theme) { return ({
    flexAlignJustifyCenter: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
    },
    height100: {
        height: '100%',
    },
}); });
//# sourceMappingURL=no-image.styles.js.map