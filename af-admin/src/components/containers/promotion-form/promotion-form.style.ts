import { createStyles } from '@mantine/core';

const useStyles = createStyles((theme) => ({
	editorLabel: {
		fontSize: '15px',
		fontWeight: 'normal',
		padding: '5px 0',
	},
	btnBox: {
		margin: '20px 0',
	},
}));

export default useStyles;
