import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
	carouselContainer: {
		margin: '4rem 0',
	},
	paginationContainer: {
		display: 'flex',
		justifyContent: 'center',
		alignItems: 'center',
	},
	slideContainer: {
		display: 'flex',
		alignContent: 'center',
		justifyContent: 'center',
	},
}));
